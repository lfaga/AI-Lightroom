<Window x:Class="AILightroom.InputBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Style="{StaticResource DefaultWindowStyle}"
        ShowInTaskbar="False" WindowStyle="ToolWindow"
        Title="InputBox" Height="160" Width="420">
  <Grid Margin="4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Label Grid.Row="0" x:Name="LblMessage" Margin="0,0,0,2" Padding="2" />
    <TextBox Grid.Row="1" x:Name="TxtInput" Margin="0,0,0,2" Padding="2" KeyDown="TxtInput_OnKeyDown" />
    <StackPanel Grid.Row="2" Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Top">
      <Button x:Name="BtnOk" Width="100" Height="32">
        <StackPanel Orientation="Horizontal">
          <Image Source="{StaticResource BmpAccept}" IsHitTestVisible="False" Stretch="None" />
          <TextBlock>Accept</TextBlock>
        </StackPanel>
      </Button>
      <Separator Width="2" />
      <Button x:Name="BtnCancel" Width="100" Height="32">
        <StackPanel Orientation="Horizontal">
          <Image Source="{StaticResource BmpCancel}" IsHitTestVisible="False" Stretch="None" />
          <TextBlock>Cancel</TextBlock>
        </StackPanel>
      </Button>
    </StackPanel>
  </Grid>
</Window>